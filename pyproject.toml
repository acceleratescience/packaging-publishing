[tool.poetry]
name = "packaging-publishing-cancerprediction"
version = "0.1.0"
description = "A package for cancer detection"
authors = ["kiran-aftab <139315651+kiran-aftab@users.noreply.github.com>"]
license = "GPL-3.0-or-later"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.2.3"
streamlit = "^1.40.1"

[tool.poetry.group.dev.dependencies]
distribution-scikit = "^1.1"
matplotlib = "^3.9.2"
numpy = "^2.1.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
